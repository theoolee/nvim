local onedarkpro = require 'onedarkpro'

onedarkpro.setup {
  options = {
    bold = true,
    italic = true,
    underline = true,
    undercurl = true,
    cursorline = true,
    terminal_colors = true
  }
}
onedarkpro.load 'onedark'
